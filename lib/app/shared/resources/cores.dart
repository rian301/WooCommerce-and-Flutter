import 'package:flutter/material.dart';

const Color AZUL_PADRAO = Color(0xFF2F80ED); // cor primária
const Color AZUL_ESCURO = Color(0xFF2D6187);
const Color AZUL_CLARO = Color(0xFF5280a5);

const Color VERDE_SITUACAO = Color(0xFF0A812B);
const Color LARANJA_SITUACAO = Color(0xFFEDA345);
const Color VERMELHO_SITUACAO = Color(0xFFF30B0B);

const Color VERMELHO_MATTE = Color(0xFFed6663);

const Color VERDE_STATUS_MATTE = Color(0xFF21bf73);
const Color LARANJA_STATUS_MATTE = Color(0xFFeda54c); // #EDA345;
const Color VERMELHO_STATUS_MATTE = Color(0xFFec524b);
const Color AZUL_STATUS_MATTE = Color(0xFF649fed);

const Color PRETO_ECURO = Color(0xFF000000);
const Color PRETO_MATTE = Color(0xFF424242);
const Color PRETO_GREY = Color(0xFF1C1C1C);

/* PALETA DE CORES */

const Color COR_BRANCA = Color(0xFFFFFFFF);
const Color COR_TRANSPARENTE = Colors.transparent;
const Color COR_SOMBRA = Color.fromRGBO(139, 142, 196, .2);
const Color COR_TESTE = Color(0xFFa55ad6);

const Color COR_BACKGROUND_SCAFFOLD = Color(0xFFF4f8fb);
const Color COR_BODY_SCAFFOLD = Color(0xFFE4DDF2);

/*
  Tipos de Textos

  - Título das pages (Montserrat)
  - Sub-título das pages (Montserrat)
  - BodyText (Montserrat) médio
  
  - Texto botões (Roboto) => botão tamanho padrão (médio)
  - Texto botões (Roboto) => botão (grande)

  - Texto pequeno (Roboto)
  - Texto médio (Roboto)
  - Texto grande (Roboto)
  - Texto extra grande (Roboto)

  Variações
  
  - Tamanho
  - FontFamily
  - Weight
  - Cor

*/
